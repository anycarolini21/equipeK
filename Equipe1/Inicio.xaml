<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Equipe1.Inicio">

    <ContentPage.Content>
        <Grid BackgroundColor="LightBlue" Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="8*" />
            </Grid.RowDefinitions>
            
            <StackLayout Grid.Row="1"
                         VerticalOptions="Center"
                         Spacing="10">
                <Button Text="Cliente"
                Clicked="TrocarDePagina" />
                <Button Text="Fornecedor" 
                Clicked="TrocarFornecedor" />
                <Button Text="Matéria-prima" />
                <Button Text="Setores" />
                <Button Text="Pedido" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
